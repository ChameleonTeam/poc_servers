name: DummyFront

on:
  push:
    branches:
      - master
      - feature/github_actions
      path: 'dummyFront/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, world!